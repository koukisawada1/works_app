<div class="post-index">
	<p>一覧</p>
	<% @posts.each do |post| %>
		<%= link_to post_path(post) do %>
			<% post.images.first(1).each do |image| %>
				<%= attachment_image_tag image, :image, fallback: "no_image.jpg", size: '450x300' %>
		 	<% end %>
		<% end %>
	<% end %>
</div>
